namespace EasyOC.Core
{
    public static class AutoMapperExtentions
    {
        //public static IServiceCollection AddAutoMapperByType<T>(this IServiceCollection services)
        //{
        //    AppDomain.CurrentDomain.GetAssemblies()
        //        .SelectMany(x => x.GetTypes()
        //                            .Where(x => x.BaseType == typeof(AppServcieBase))
        //}
    }
}
